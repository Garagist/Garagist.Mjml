prototype(Neos.Neos:ContentComponent) {
    @if.mjmlVisibility = Garagist.Mjml:Helper.Visibility
}

prototype(Neos.Neos:Content) {
    @if.mjmlVisibility = Garagist.Mjml:Helper.Visibility
}

prototype(Garagist.Plaintext:Page) {
    prototype(Garagist.Mjml:Helper.Visibility) {
        inBackend = false
        onNewsletter = true
    }
}
